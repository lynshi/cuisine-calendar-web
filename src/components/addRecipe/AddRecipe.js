import styles from './AddRecipe.module.css'

function AddRecipe() {
    return (
        <div className={styles.body}>This is the planning calendar placeholder</div>
    )
}

export default AddRecipe;